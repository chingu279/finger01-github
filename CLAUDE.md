# 작업 지침

개인 건강 관리 멀티에이전트 시스템. 상세 설계는 `docs/` 참고.

## 절대 규칙

1. **건강 데이터를 커밋하지 않는다.** `data/**` 는 `.gitignore` 대상이다.
   이 줄을 지우거나 예외를 추가하지 않는다. 커밋 전 `git status --short` 로 확인한다.
2. **숫자를 계산하지 않는다.** `./health brief` / `score --json` 의 출력을 인용한다.
   추가 집계가 필요하면 스크립트를 작성해 실행한다. 암산 결과는 신뢰할 수 없다.
3. **트리아지 판정을 낮추지 않는다.** `triage.py` 의 심각도는 에이전트가 완화할 수 없다. 상향만 가능하다.
4. **진단하지 않는다.** "~일 수 있으니 확인이 필요하다"까지가 한계다.
   약물 용량 조절, 처방약 중단 권유, "병원 갈 필요 없다"는 판단은 금지다.
5. **데이터가 없으면 "괜찮다"고 하지 않는다.** "판단 보류"라고 말한다.
   거짓 안심이 이 시스템의 가장 위험한 실패다.

## 안전 코드를 고칠 때

`src/health/triage.py` 는 특별 취급한다.
- 모든 변경에 **회귀 테스트를 함께 추가**한다 (`tests/test_health.py`).
- 기존 레드플래그 테스트를 삭제하지 않는다.
- EMERGENCY 규칙은 오탐(경보 피로)을 이유로 완화하지 않는다. 미탐이 훨씬 비싸다.
- 규칙 완화 시 근거를 커밋 메시지에 남긴다.

커밋 전에는 항상:
```bash
python3 -m pytest tests -q
```

## 웹 UI 를 고칠 때

- **`127.0.0.1` 바인딩을 바꾸지 않는다.** `0.0.0.0` 으로 열면 같은 네트워크의
  누구나 건강기록을 본다. 회귀 테스트가 이걸 막고 있다.
- 검증은 웹과 CLI 가 **같은 코드**(`checkin.RANGES`, `Prompt.parse`)를 쓴다.
  웹에만 따로 검증을 두면 한쪽이 반드시 뒤처진다.
- 상태색(good/warning/serious/critical)은 예약색이다. 계열색으로 쓰지 않고,
  **항상 아이콘 + 라벨과 함께** 쓴다. 색만으로 의미를 나르지 않는다.
- 그래프의 결측은 선을 끊는다. 이어 그리면 재지 않은 날을 잰 것처럼 보인다.
  대부분의 점이 고립된 계열(혈압처럼)은 점으로 그린다.

## 코드 규약

- 코어(`src/health/`)는 **표준 라이브러리만** 쓴다. 의존성을 추가하지 않는다.
- 결측은 예외가 아니라 정상 상태다. **결측을 0으로 채우지 않는다.**
  (안 잔 것과 기록 안 한 것은 다르다.)
- 새 지표를 추가할 때 단위를 필드명에 박는다 (`_min`, `_ms`, `_mg`).
- `baseline.TRACKED` 에 지표를 추가할 때 `sd_floor`(의미 있는 최소 변화폭)를 함께 정한다.
- 주석은 "무엇을"이 아니라 **"왜"**를 쓴다. 특히 임계값과 상수에는 근거를 남긴다.

## 에이전트를 추가하려 할 때

세 질문을 통과해야 한다.
1. 기존 에이전트 프롬프트를 늘려서 되는가? → 되면 추가하지 않는다.
2. 실패 모드가 기존과 다른가? → 다르면 분리할 가치가 있다.
3. **결정적 코드로 만들 수 있는가? → 만들 수 있으면 코드로 만든다.**

## 자주 쓰는 명령

저장소 루트의 `./health` 진입점을 쓴다. `python` 을 직접 부르지 않는다 —
macOS 에는 `python` 이 없어서 문서의 명령을 그대로 따라 치면 실패한다.

| 명령 | 하는 일 |
|---|---|
| `./health checkin` | 대화형 일일 체크인 |
| `./health status` | Phase 0 게이트 판정 |
| `./health brief` | 일일 팩트시트 (에이전트 입력) |
| `./health score --json` | 준비도 |
| `./health triage --json` | 레드플래그. 종료코드가 심각도 |
| `./health weekly` | 주간 팩트시트 |
| `./health serve` | 로컬 웹 체크인 화면 (127.0.0.1 전용) |
| `./health seed --days 30` | 데모 데이터 (합성. 실제 값 아님) |
| `python3 -m pytest tests -q` | 테스트 |
